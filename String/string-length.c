#include<stdio.h>
#include<string.h>
int main(){
        char Name[]= "Anish" ;
        char a[20];
        printf("the string length is:%zu\n",strlen(Name));
        strcpy(Name,a);
        printf("The String is:%zu\n",a);
}